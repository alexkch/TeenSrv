<!-- Nav bar -->

<nav class="navbar navbar-inverse navbar-fixed-bottom">
    <div class="container-fluid">
      <div class="row">
          
      <div class="col-md-2">     
          <span class="nav_trigger" style="float:left">
              <i class="fa fa-user"></i>
          </span>
        </div>
        <div class="col-md-7">
            <form class="navbar-form" style="float:right" role="search" method="get" action="search.php">
              <div class="form-group">
                <input type="text" class="form-control" name="q" placeholder="Search for jobs" style="width:500px;">
              </div>
              <button type="submit" class="btn btn-default"><span class="fa fa-search"></span></button>
            </form>
        </div>
        <div class="col-md-3">
          <ul class="nav navbar-nav navbar-right">

              <li class="dropdown">
                <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-expanded="false"><i class="fa fa-envelope-o"></i>Contact Us</a>
                  <ul class="dropdown-menu" role="menu">
                      <li><a href="#"><span class="fa fa-facebook-square"></span> Our Facebook</a></li>
                      <li><a href="#"><span class="fa fa-twitter-square"></span> Our Twitter</a></li>
                    <li><a href="#"><span class="fa fa-youtube-play"></span> Our Youtube</a></li>
                    <li class="divider"></li>
                    <li><a href="#"><span class="fa fa-share"></span> Contact Us</a></li>
                </ul>
              </li>
          </ul>
      </div>
      </div>
  </div>
</nav>
<!-- ./bottom menu bar -->
<!-- Sidebar -->
<div id="push_sidebar">
<h2> Signed in as <%= @user.username %> </h2>
  <ul class="list-unstyled">
    <li><a href="/"><span class="fa fa-home"></span>Home</a></li>

    <% if(current_user.usertype==0) %>
      <% @teenager = Teenager.find_by_user_id(current_user.id) %>
      <% if(@teenager!=nil) %>
        <li><span class="fa fa-id-card"></span><%= link_to 'Profile', user_teenager_path(current_user.id, @teenager.id) %></li>
      <% else %>
        <li><span class="fa fa-id-card"></span><%= link_to 'Create Profile', new_user_teenager_path(current_user.id) %></li>
      <% end %>

    <% elsif(current_user.usertype==1) %>
      <% @client = Client.find_by_user_id(current_user.id) %>
      <% if(@client != nil) %>
        <li><span class="fa fa-id-card"></span><%= link_to 'Profile', user_client_path(current_user.id, @client.id) %></li>
      <% else %>
        <li><span class="fa fa-id-card"></span><%= link_to 'Create Profile', new_user_client_path(current_user.id) %></li>
      <% end %>
    <% end %>
    <li><a href="/newbuilding.php"><span class="fa fa-plus"></span>Post New Jobs</a></li>
  <li><a href="/updateuser.php"><span class="fa fa-pencil"></span>Update Account Information</a></li>
  <li><%= link_to(new_password_path(@user)) do %><span class="fa fa-unlock-alt"></span>Change Account Password</a></li><% end %>
  <li><%= link_to(destroy_user_session_path, method: :delete) do %><span class="fa fa-power-off"></span>Log Out</a></li><% end %>
  </ul>
</div>