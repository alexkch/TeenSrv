<%= form_with(model: [@user, @endorsement], local: true) do |form| %>

  <% if @endorsement.errors.any? %>
    <div id="error_explanation">
      <h2>
        <%= pluralize(@endorsement.errors.count, "error") %> prohibited
        this endorsement from being saved:
      </h2>
      <ul>
        <% @endorsement.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
 
  <p>
    <%= form.label :comment %><br>
    <%= form.text_area :comment %>
  </p>
 
  <p>
    <%= form.submit %>
  </p>
  
 <% end %>